{
    "buildCommand": "npx expo export --platform web && cp -r public/* dist/",
    "outputDirectory": "dist",
    "cleanUrls": true,
    "framework": null,
    "rewrites": [
        {
            "source": "/:path*",
            "destination": "/"
        }
    ]
}